---
import { renderUiIcon, type UiIconName } from "../lib/icons";

type Props = {
  name: UiIconName;
  size?: number;
  class?: string;
};

const { name, size = 18, class: className = "" } = Astro.props as Props;
const svg = renderUiIcon(name, size, className);
---

<Fragment set:html={svg} />
